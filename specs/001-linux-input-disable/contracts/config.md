# 接口契约: 配置文件

## 目的

定义用户可配置的启用状态与设备识别规则，以便持久化行为。

## 配置项（逻辑结构）

- **enabled**: 是否启用自动禁用功能（true/false）
- **device_rules**: 设备识别规则集合
  - **external**: 外置设备识别规则（按类型）
  - **internal**: 内置设备识别规则（按类型）

## 行为约束

- 配置更新后需在合理时间内生效（不要求重启服务）
- 解析失败时不得改变当前启用状态与设备禁用状态
- 未配置规则时使用默认识别策略
